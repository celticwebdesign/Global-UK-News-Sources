[build]
  publish = "."

[functions]
  node_bundler = "esbuild"

[context.production.environment]
  NODE_VERSION = "20"

# Clean URL redirect for the proxy function
[[redirects]]
  from = "/api/proxy"
  to = "/.netlify/functions/proxy"
  status = 200
